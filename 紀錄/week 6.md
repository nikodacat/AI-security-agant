# AI資安
針對linux/ubuntu進行兼容整合，並且做測試
*本來原本也要灌windows來做空機和其他測試，但win10跟win11好像都和vm不合，到時候去拿另一台電腦再灌

對伺服器的檢測計畫:(未實作)
因為到目前為止是以居家電腦為範圍，所以並沒有做code scanning的部分，也就是目前完全只能透過電腦設定和安裝插件來進行安全掃描，沒有針對居家電腦幾乎見不到的網站架設。
需要一個計畫來處理用戶自己編寫的潛在攻擊面(網站漏洞之類的)。
1.以外部攻擊來發現弱點，從前幾周有提到過的ctf方式，讓pentestgpt將網站視為入侵目標，透過現有的專題來檢測漏洞。
2.開發一個以llm為主的程式掃描器，在內部透過掃描程式的方式來尋找潛在漏洞，這個跟現有的coding llm還算契合，只要放進檢測的流水線就能完成(要一起包含回報在某些設定下可能會遭到攻擊的漏洞，以方便統整模型做出進一步的診斷)。


# 音樂遊戲譜面生成

先
